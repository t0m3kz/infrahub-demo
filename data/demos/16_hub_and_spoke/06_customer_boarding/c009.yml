---
# C009 (Tavelo Inc.) — TopologyCustomerCloud footprint on Azure westeurope,
# namespace-only (see ./c005.yml for the AWS equivalent and the
# human_friendly_id note on why owners must be distinct).
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: TopologyCustomerCloud
  data:
    - owner:
        kind: OrganizationCustomer
        data:
          name: "Tavelo Inc."
          org_id: "C009"
      customer_name: "hub-and-spoke-azure"
      environment: "p"
      status: active
      parent: "westeurope"
