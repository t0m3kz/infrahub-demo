---
# Pre-create shared NTP/syslog/AAA IPs referenced by colocation capabilities files.
# These must load before any 05_capabilities.yml in this directory tree.

apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: IpamIPAddress
  data:
    - address: "162.159.200.1/32"
      ip_namespace: default

    - address: "162.159.200.123/32"
      ip_namespace: default

    - address: "162.159.210.11/32"
      ip_namespace: default

    - address: "10.0.0.100/32"
      ip_namespace: default

    - address: "10.0.0.200/32"
      ip_namespace: default

    - address: "10.0.0.201/32"
      ip_namespace: default
