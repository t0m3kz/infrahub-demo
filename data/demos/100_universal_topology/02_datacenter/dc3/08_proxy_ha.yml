---
# ── HA domain ─────────────────────────────────────────────────────────────────
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: ManagedProxyHA
  data:
    - name: DC3-PRX-HA
      group_id: 3
      mode: active-passive
      priority: 100
      preempt: false
      proxy_type: explicit
      proxy_vendor: haproxy
      status: active
      description: "DC3 HAProxy explicit-proxy HA pair — tenant egress inspection"
      capabilities:
        - DC3-PRX-01
        - DC3-PRX-02
