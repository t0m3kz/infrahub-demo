---
# C002 (SwiftGo GmbH) — Applications
#
# shipment-tracker:  real-time parcel & freight tracking (production + nonprod)
# fleet-manager:     vehicle fleet dispatch and telematics (production only)

apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: AppApplication
  data:
    - label: shipment-tracker
      environment: p
      criticality: high
      security_profile: internet_exposed
      fqdn: shipment-tracker.c002.demo.local
      ingress_mode: frontend
      parent: C002
      member_of_groups:
        - app_applications

    - label: shipment-tracker
      environment: n
      criticality: low
      security_profile: internet_exposed
      fqdn: shipment-tracker-np.c002.demo.local
      ingress_mode: frontend
      parent: C002
      member_of_groups:
        - app_applications

    - label: fleet-manager
      environment: p
      criticality: high
      security_profile: internet_exposed
      fqdn: fleet-manager.c002.demo.local
      ingress_mode: frontend
      parent: C002
      member_of_groups:
        - app_applications
