---
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: CoreNodeTriggerRule
  data:

    # - name: trigger-dc-generator-on-merge
    #   branch_scope: "default_branch"
    #   node_kind: TopologyDataCenter
    #   mutation_action: "created"
    #   action: run-dc-generator

    - name: trigger-dc-generator-update-super-spines
      branch_scope: "other_branches"
      node_kind: TopologyDataCenter
      mutation_action: "updated"
      action: run-dc-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: amount_of_super_spines
            value_match: any

    - name: trigger-dc-generator-update-super-spine-template
      branch_scope: "other_branches"
      node_kind: TopologyDataCenter
      mutation_action: "updated"
      action: run-dc-generator
      matches:
        kind: CoreNodeTriggerRelationshipMatch
        data:
          - relationship_name: super_spine_template
            modification_type: updated

    - name: trigger-dc-generator-status
      branch_scope: "other_branches"
      node_kind: TopologyDataCenter
      mutation_action: updated
      action: run-dc-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: status
            value_match: value_full
            value: active
            value_previous: provisioning

    # Pod/rack creation no longer needs an event trigger: add_dc/add_pod fan out
    # to add_pod/add_rack directly via CoreGeneratorDefinitionRun(wait_until_completion=true)
    # at the end of their own generate() (see CommonGenerator.run_generator_and_wait),
    # so the child generator only ever runs once its parent's data already exists —
    # no checksum write, no readiness polling on the child's side.

    - name: trigger-pod-generator-update-index
      branch_scope: "other_branches"
      node_kind: TopologyPod
      mutation_action: "updated"
      action: run-pod-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: index
            value_match: any

    - name: trigger-pod-generator-update-amount-of-spines
      branch_scope: "other_branches"
      node_kind: TopologyPod
      mutation_action: "updated"
      action: run-pod-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: amount_of_spines
            value_match: any

    # Rack creation is started by its parent (see comment above trigger-pod-generator-update-index).
    # A network rack's own tor/compute row-dependents are, in turn, started by that
    # network rack's generate() (RackGenerator._fan_out_to_row_dependent_racks).

    - name: trigger-rack-generator-update-index
      branch_scope: "other_branches"
      node_kind: LocationRack
      mutation_action: "updated"
      action: run-rack-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: index
            value_match: any

    - name: trigger-rack-generator-update-rack-type
      branch_scope: "other_branches"
      node_kind: LocationRack
      mutation_action: "updated"
      action: run-rack-generator
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: rack_type
            value_match: any

    - name: trigger-rack-generator-update-templates
      branch_scope: "other_branches"
      node_kind: LocationRack
      mutation_action: "updated"
      action: run-rack-generator
      matches:
        kind: CoreNodeTriggerRelationshipMatch
        data:
          - relationship_name: fabric_templates
            modification_type: updated

    # # Device decommission: when status set to decommissioned
    # - name: trigger-device-decommission
    #   branch_scope: "other_branches"
    #   node_kind: DcimPhysicalDevice
    #   mutation_action: "updated"
    #   action: run-decommission-device
    #   matches:
    #     kind: CoreNodeTriggerAttributeMatch
    #     data:
    #       - attribute_name: status
    #         value_match: decommissioned

    # Pod decommission: when status set to decommissioned
    - name: trigger-pod-decommission
      branch_scope: "other_branches"
      node_kind: TopologyPod
      mutation_action: "updated"
      action: run-decommission-pod
      matches:
        kind: CoreNodeTriggerAttributeMatch
        data:
          - attribute_name: status
            value_match: value_full
            value: decommissioned
            value_previous: active
            # match_type: exact

    # VRF namespace: generator auto-triggers via group membership (execute_in_proposed_change: true)
    # No deployment-based trigger needed — L3 VNI comes from global pool

    # Segment triggers: re-run generator when deployments are added/updated
    - name: trigger-vlan-segment-deployment-updated
      branch_scope: "other_branches"
      node_kind: ManagedVlanSegment
      mutation_action: "updated"
      action: run-vlan-segment-generator
      matches:
        kind: CoreNodeTriggerRelationshipMatch
        data:
          - relationship_name: deployment
            modification_type: updated

    - name: trigger-vxlan-segment-deployments-updated
      branch_scope: "other_branches"
      node_kind: ManagedVxlanSegment
      mutation_action: "updated"
      action: run-vxlan-segment-generator
      matches:
        kind: CoreNodeTriggerRelationshipMatch
        data:
          - relationship_name: deployments
            modification_type: updated

            # Endpoint trigger when server device added to endpoints

    # ── MLAG Wiring ───────────────────────────────────────────────────────────
    - name: trigger-mlag-on-update
      branch_scope: "other_branches"
      node_kind: ManagedMLAG
      mutation_action: "updated"
      action: mlag-generator

    # ── HA Interfaces ─────────────────────────────────────────────────────────
    - name: trigger-ha-on-firewall-update
      branch_scope: "other_branches"
      node_kind: ManagedFirewallHA
      mutation_action: "updated"
      action: ha-generator

    - name: trigger-ha-on-lb-update
      branch_scope: "other_branches"
      node_kind: ManagedLoadbalancerHA
      mutation_action: "updated"
      action: ha-generator
    # ── App Security Rules ────────────────────────────────────────────────────
    # Single trigger path: AppApplication events invoke the canonical app generator.

    - name: trigger-app-security-rules-on-application-created
      branch_scope: "other_branches"
      node_kind: AppApplication
      mutation_action: "created"
      action: run-app-application-generator
