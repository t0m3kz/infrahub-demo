---
version: "1.0"

nodes:
  - name: Loadbalancer
    namespace: Onprem
    description: "On-premises load balancer (e.g., F5, HAProxy) hosting VIP services."
    label: "On-Prem Load Balancer"
    icon: carbon:floating-ip
    include_in_menu: false
    human_friendly_id:
      - name__value
    display_label: "{{ name__value }}"
    inherit_from:
      - ManagedLoadBalancer
      - ManagedGeneric
      - CoreArtifactTarget
    relationships:
      - name: deployment
        peer: TopologyDeployment
        cardinality: one
        kind: Attribute
        order_weight: 500
        optional: false
        on_delete: no-action
        description: "The deployment location for this load balancer"
