---
version: "1.0"
infrahub_tests:
  # ==================== CONFIG QUERIES ====================
  - resource: GraphQLQuery
    resource_name: leaf_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/leaf.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: spine_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/spine.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: edge_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/edge.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: oob_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/oob.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: console_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/console.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: firewall_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/firewall.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: loadbalancer_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/loadbalancer.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: border_leaf_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/border_leaf.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: super_spine_config
    tests:
      - name: syntax_check
        spec:
          path: queries/config/super_spine.gql
          kind: graphql-query-smoke

  # ==================== TOPOLOGY QUERIES ====================
  - resource: GraphQLQuery
    resource_name: topology_dc
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/dc.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_pod
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/pod.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_rack
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/rack.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_cabling
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/cabling.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_pop
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/pop.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_equinix_pop
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/equinix_pop.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_simulator
    tests:
      - name: syntax_check
        spec:
          path: queries/topology/clab.gql
          kind: graphql-query-smoke

  # ==================== SERVICE QUERIES ====================
  - resource: GraphQLQuery
    resource_name: virtual_fabric_validation
    tests:
      - name: syntax_check
        spec:
          path: queries/service/virtual_fabric_validation.gql
          kind: graphql-query-smoke
