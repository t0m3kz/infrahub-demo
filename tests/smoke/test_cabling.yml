---
version: "1.0"
infrahub_tests:
  - resource: PythonTransform
    resource_name: topology_cabling
    tests:
      - name: baseline
        expect: PASS
        spec:
          kind: python-transform-unit-process
          directory: cabling
