---
# Throwaway customer for the app-catalogue integration scenario — deliberately
# NOT reusing C001 (data/demos/16_hub_and_spoke) so this test has no dependency
# on the DC/customer-boarding demo chain.
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: OrganizationCustomer
  data:
    - name: "IT001 Test Customer"
      org_id: "IT001"
