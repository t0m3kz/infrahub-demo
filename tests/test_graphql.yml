---
version: "1.0"
infrahub_tests:
  # --------------------  GraphQLQuery  --------------------
  - resource: GraphQLQuery
    resource_name: firewall_config
    tests:
      - name: syntax_check
        spec:
          path: templates/firewall_config.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: pop_config
    tests:
      - name: syntax_check
        spec:
          path: templates/pop_config.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_dc
    tests:
      - name: syntax_check
        spec:
          path: generators/topology_dc.gql
          kind: graphql-query-smoke

  - resource: GraphQLQuery
    resource_name: topology_simulator
    tests:
      - name: syntax_check
        spec:
          path: simulators/topology.gql
          kind: graphql-query-smoke