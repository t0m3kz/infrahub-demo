query app_service_ports($slug: String!) {
  AppComponent(slug__value: $slug) {
    edges {
      node {
        id
        slug { value }
        vip_service {
          node {
            id
            port { value }
            protocol { value }
          }
        }
      }
    }
  }
}
